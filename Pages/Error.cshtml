@page
@model ErrorModel
@{
    ViewData["Title"] = "Errore";
}

<div class="container">
    <div class="card">
        <div class="card-body text-center py-5">
            <div class="mb-4">
                <i class="fas fa-exclamation-circle text-danger" style="font-size: 4rem;"></i>
            </div>

            <h1 class="card-title mb-3">Si è verificato un errore</h1>
            <p class="card-text mb-4">Ci scusiamo per l'inconveniente. Si è verificato un problema durante l'elaborazione della tua richiesta.</p>

            @if (Model.ShowRequestId)
            {
                <div class="alert alert-info d-inline-block mx-auto mb-4">
                    <strong>ID Richiesta:</strong> <code>@Model.RequestId</code>
                </div>
            }

            <div class="mt-4">
                <a href="/" class="btn btn-primary">
                    <i class="fas fa-home me-2"></i> Torna alla Home
                </a>
            </div>
        </div>
    </div>

    @if (System.Diagnostics.Debugger.IsAttached || Environment.GetEnvironmentVariable("ASPNETCORE_ENVIRONMENT") == "Development")
    {
        <div class="card mt-4">
            <div class="card-header bg-warning text-dark">
                <i class="fas fa-code me-2"></i> Modalità Sviluppo
            </div>
            <div class="card-body">
                <p>
                    Passare all'ambiente <strong>Development</strong> mostra informazioni dettagliate sull'errore che si è verificato.
                </p>
                <p>
                    <strong>L'ambiente Development non dovrebbe essere abilitato per le applicazioni in produzione.</strong>
                    Potrebbe comportare la visualizzazione di informazioni sensibili dalle eccezioni agli utenti finali.
                    Per il debug locale, abilita l'ambiente <strong>Development</strong> impostando la variabile di ambiente <strong>ASPNETCORE_ENVIRONMENT</strong> su <strong>Development</strong>
                    e riavviando l'applicazione.
                </p>
            </div>
        </div>
    }
</div>
